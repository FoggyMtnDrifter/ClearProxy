<script lang="ts">
  /**
   * @component RootPage
   * @description Root/index page of the application
   *
   * This page is a simple placeholder that doesn't contain any client-side logic
   * because all routing and redirection logic is handled by the server hooks
   * in hooks.server.ts, which redirects users to the appropriate page based on
   * their authentication status.
   */
  // No client-side logic needed as redirection is handled by hooks.server.ts
</script>
